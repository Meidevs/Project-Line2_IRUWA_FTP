const rooms = [];

const addUser = () => {
  
}


const getUser = (uid) => users.find((user) => user.userID === uid);

const setMessages = ( roomCode, userID, message ) => {
  
}

module.exports = { addUser, removeUser, getUser, setMessages };